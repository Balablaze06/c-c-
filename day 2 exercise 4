#include<stdio.h>
int main()
{
    int limit;
    int cube;
    printf("Input number of terms :");
    scanf("%d",&limit);
    printf(" %d",limit);
    for(int i=1; i<=limit; i++)
    {
        cube = i * i * i;
        printf("\nNumber is : %d and cube of the %d is :%d",i,i,cube);
    }
}

#include<stdio.h>
#include<string.h>
int main() {
    char str[10], str1[10];
    scanf("%s",str);
    strcpy(str1,str); /* copying from source to destination */
    printf("Source string %s and the destination string %s",str,str1);
}
